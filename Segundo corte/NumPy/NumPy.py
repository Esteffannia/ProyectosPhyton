# Se puede hacer referencia al paquete NumPy como np en lugar de numpy
import numpy as np

arr = np.array([1, 2, 3, 4, 5])

print(arr)

# Comprobando la versión de NumPy
print(np.__version__)
